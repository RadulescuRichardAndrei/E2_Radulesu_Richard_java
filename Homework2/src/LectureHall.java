public class LectureHall extends Room{
  public static boolean videoP;

  /**
   * This is the constructor of the LectureHall class
   * @param n a string containing the name of the LectureHall
   * @param cap the capacity of the LectureHall
   */
  public LectureHall(String n, int cap) {
    super(n, cap);
  }
}
