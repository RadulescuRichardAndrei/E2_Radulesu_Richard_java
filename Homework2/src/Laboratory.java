public class Laboratory extends Room{
    public static String op;
    /**
     * This is the constructor of the Laboratory class
     * @param n a string containing the name of the Laboratory
     * @param cap the capacity of the Laboratory
     */
    public Laboratory(String n, int cap) {
        super(n, cap);
    }
}
